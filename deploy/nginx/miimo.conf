server {
    server_name mimonatural.com www.mimonatural.com;

    client_max_body_size 6M;

    location / {
        proxy_pass http://127.0.0.1:3010;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    error_log /var/log/nginx/miimo_error.log;
    access_log /var/log/nginx/miimo_access.log;

    listen 443 ssl;
    ssl_certificate /etc/letsencrypt/live/mimonatural.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/mimonatural.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
}

server {
    listen 80;
    server_name mimonatural.com www.mimonatural.com;
    return 301 https://$host$request_uri;
}
